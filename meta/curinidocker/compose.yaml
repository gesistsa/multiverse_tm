services:
  r36:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - type: bind
        source: ../../.
        target: /root/multiverse_tm
    environment:
      - INSIDEDOCKER=1
